import UpdateAddressForm from '../../../../components/UpdateAddressForm';

const AddressUpdate = ({ onSubmit, initialValue }) => {
  return (
    <div className="update-container">
      <UpdateAddressForm />
    </div>
  );
};

export default AddressUpdate;
